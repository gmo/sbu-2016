<%= form_for(@keyword) do |f| %>
  <% if @keyword.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@keyword.errors.count, "error") %> prohibited this keyword from being saved:</h2>

      <ul>
      <% @keyword.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <style>
  p.one {
    padding-left: 138px;
  }
  </style>

  <div class="field">
    <%= f.label :word %>
    <p style="display:inline" class="one"></p>
    <%= f.label :language %><br>
    <%= f.text_field :word %>
    
    <%= f.select(:language) do %>
      <% options_for_select([['Arabic', 'Arabic'], ['Chinese', 'Chinese'], ['English', 'English'], ['French', 'French'], ['Hindi', 'Hindi'], ['Indonesian', 'Indonesian'], ['Portuguese', 'Portuguese'], ['Russian', 'Russian'], ['Spanish', 'Spanish']])%>
    <% end %>
  </di>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
